<!-- **********************************************
     * Error View
     * --------------------------------------------
     * Displays error messages for server or client
     * errors (500 or 404).
     * - Uses <%- message %> for raw HTML support.
     * - Provides specific messages for 500 and 404.
     * - Includes link back to the home page.
     ********************************************** -->

<main>
  <h1><%= title %></h1>

  <div class="error-message">
    <p><%- message %></p>

    <% if (title === 500 || title === '500') { %>
      <p>It looks like something went wrong on our end. We're working on it!</p>
    <% } else if (title === 404 || title === '404') { %>
      <p>We couldn't find the page you're looking for. Please check the URL or return home.</p>
    <% } %>

    <a href="/">Back to safety →</a>
  </div>
</main>
