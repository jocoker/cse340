<!-- **********************************************
     * Classification View
     * --------------------------------------------
     * Displays all vehicles for a specific classification.
     * - Shows the page title if available; otherwise redirects to home.
     * - Uses 'grid' variable to render the inventory list (HTML injected).
     * - 'grid' is expected to be generated by utilities.buildClassificationGrid().
     ********************************************** -->

<% if (title) { %>
  <h1><%= title %></h1>
<% } else { %>
  <% res.redirect('/') %>
<% } %>

<%- grid %> <!-- Inventory grid HTML is injected directly -->
